import type { ReactElement } from "react";
import styles from "./styles/RecordedClassView.module.scss";

export default function RecordedClassView(): ReactElement {
  return (
    <>
      <h1 className={styles.title}>RECORDED CLASS VIEW</h1>
    </>
  );
}
